<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## Sourcing project copy

Before drafting taglines/descriptions for the featured projects in `lib/projects.ts`, run `/ask` for each project to pull recent session summaries and weekly rollups from the prompt-history store. Those reflect current direction and active work; the in-repo READMEs often lag (or are still create-next-app boilerplate). Confirm copy with Nico before committing.

Projects (slug → repo if different): musicforge, prntd, rocksculpture (repo: `~/src/showcase`), ibuild4you, prompt-lab. (lojong removed from the site 2026-07-05 — portfolio focus.) MDX files set an explicit `historyKey` when slug ≠ repo dir name.

## Where editable text lives

See `docs/editing.md` for the canonical map. All visible copy lives in `content/`: pages are MDX (`home.mdx`, `connect.mdx`, `about.mdx`, `projects/*.mdx`) and site-wide strings (title, description, footer tagline, nav labels) are in `content/site.ts`. Site is live at `https://pianohouseproject.org` (Vercel).

`site.tagline` and `site.description` are deliberately different strings: the tagline is the terse form drawn on the OG card, the description is the prose form for `<meta name="description">` and the text beneath social cards. Don't collapse them — the list form makes a poor search snippet.

**Naming.** Lowercase "the piano house project" is *this site*; Title Case "the Piano House Project" is the umbrella for all of Nico's work. "the Piano House" (in `about.mdx`) is the actual house. All three are correct in their place.

## Cross-agent handoff

This repo coordinates with `prompt-lab` (the producer of `public_session_summaries` / `public_weekly_rollups`) via an append-only shared file at `~/src/.handoff/selected-projects-prompt-lab.md`. **Read it at session start** alongside `/readup`. New cross-repo asks (bug reports about the public tables, schema changes, expected-behavior clarifications) go there as a new entry under `## Active`. When an entry is acted on, move it under `## Archived` with a one-line outcome. See the file's own header for the entry format. Cross-repo GitHub issues remain valid for anything the public web should see; the handoff file is for working-state coordination.

## Environments

See `docs/environments.md`. Local dev uses `file:dev.db` (init: `npm run db:migrate`); preview deploys use the `pianohouse-preview` Turso DB; only production touches the prod DB (`pianohouse`). Prod smoke tests use plus-addressed accounts (`nlovejoy+<label>@me.com`), cleaned up after.

## Local checks

`npm run check` validates content/ — project frontmatter, status/category enums, `image`/`cardImage` references resolve under `public/`, `lib/projects.ts` imports every `.mdx` file. A `simple-git-hooks` pre-push runs `check` + `next build` automatically; bypass with `SKIP_SIMPLE_GIT_HOOKS=1 git push` if you really need to.

## Next steps

Older "Done" entries archived in `docs/roadmap-archive.md` (everything before the two blocks below).

**Gotchas learned 2026-07-19 (afternoon):** (1) **A squash merge silently dropped three commits** — PR #8's recorded head went stale, GitHub squashed without them, and prod shipped without the dark-mode fixes. Always `git diff <branch-tip> main` after merging, and check `gh pr view --json headRefOid` matches local HEAD before. (2) `git add -A` swept a subagent's screenshot into a commit; root `*.png` is now gitignored. (3) An agent ran `npm run db:migrate` against the remote Turso DB because `.env.local` was left pointing at `libsql://` after a prod-access swap — hence the `--remote` guard.

**Done 2026-07-19 (evening — `6efec97`, live on prod):** OG share card fixed after a bad iMessage render. The wordmark was four flex divs relying on `gap`; Satori rendered the breaks unevenly and collapsed "piano house" into one word. Now three spans with the spaces baked in — **`flexShrink: 0` + `whiteSpace: nowrap` are load-bearing**, or Satori wraps the bold span onto a second line. Size 100 → 90 so "project" clears the right padding. Copy: dropped "learning the tooling is the point" and the Claude Code namedrop from the tagline, footer, and `home.mdx` intro; split `site.tagline` (terse, on the card) from `site.description` (prose, for meta + social-card body). Capitalization convention settled and written into "Where editable text lives" above. Wordmark lockup deliberately left alone.

**Gotchas learned 2026-07-19 (evening):** (1) **Never trust OG-image code by reading it** — render the PNG. The first fix here made the card *worse* (Satori wrapped the bold span onto two lines) and only rendering caught it. `curl localhost:3000/opengraph-image` in dev, then Read the file. (2) A Vercel production deploy sat in **"Initializing" with a 0ms build for ~10 minutes** — their queue stalling, not our code. It cleared on its own; don't redeploy reflexively. (3) The `next/font` PostToolUse hint on `app/opengraph-image.tsx` is a **false positive** — `ImageResponse` needs raw font ArrayBuffers, which `next/font` doesn't expose. Ignore it.

**Done 2026-07-19 (late — docs + cleanup):** (1) **Docs audit** (`81a4eaa`). `editing.md` had drifted worst — wrong `footerTagline` example, nav labels predating the `/projects` retirement, `home.mdx` called a hero, and a `git add -A` recipe contradicting a logged gotcha. `catalog-family-alignment.md` was a work list whose four items all shipped in #8; rewritten as the standing design contract with the catalog, which is what issue #7 needs from it. `assets/README.md` described the pre-redesign detail layout (`image` is an og-preview *fallback*, not a screenshot slot). `environments.md`, `secrets.md`, `README.md` verified accurate and left alone. (2) **Home hero deleted** (`f86ffd5`, −370 lines) — `app/page.tsx` stopped rendering a hero in #9, leaving a closed loop: `heroes.ts` held tuning values, `/dev/hero-tune` wrote them, `check-content.mjs` validated them, nothing read them. Removed the registry, the whole `app/dev` tree, `public/sunset.jpg`, and the validator block; `assets/originals/` left on disk (gitignored photo sources, not build inputs). Verified via check + `tsc --noEmit` + `next build` + the 6-spec Playwright suite. (3) **readup's CI probe fixed** in prompt-lab (`540290f`) — it did `2>/dev/null || echo '[]'`, so a transient `gh` failure was indistinguishable from a repo with no CI, and it reported this repo's *green* CI as absent. Now emits `CI_PROBE=skip|ok|error`, retries once, and flags workflow-files-but-zero-runs.

**Done 2026-07-21 — issue #12, split-pane live preview:** `embed: true` opt-in frontmatter (ibuild4you, prntd, musicforge, rocksculpture; validated in `check-content.mjs` — requires `url`). `app/projects/[slug]/live-preview.tsx`: one more mono-underlined action in the actions row; desktop (lg+) swaps the page into iframe-left-2/3 / detail-right-1/3 via `?preview=1`, below lg it's a plain external link. Playwright suite: 5 specs in `e2e/live-preview.spec.ts`.

**Gotchas learned 2026-07-21 (issue #12):** (1) **`router.replace`/`push` is a real navigation on a dynamic route, not a shallow URL update.** This route is force-dynamic (`getSessionUser()` reads `cookies()`), so calling the Next router to toggle `?preview=1` round-tripped to the server and raced/reverted the query param under load. Fixed with `window.history.replaceState` directly — Next patches it to keep `usePathname`/`useSearchParams` in sync everywhere, with no refetch (the documented "shallow routing" pattern, `node_modules/next/dist/docs/01-app/02-guides/single-page-applications.md`). (2) **A plain `<button>` has no pre-hydration fallback.** Unlike this repo's existing tests (which all click real `<Link>` `<a href>` elements — actionable via the browser before React attaches anything), a click on a bare `<button>` that lands before hydration completes is silently swallowed: Playwright confirms the DOM node is clickable, not that a listener exists yet. Symptom was ~20% flaky "URL never changed, zero console output" failures. Fixed test-side with a self-retrying click-then-assert helper (`toPass`) instead of leaning on `networkidle`. (3) **Tailwind v4's container-query breakpoint scale (`@sm`=24rem) is narrower than its viewport scale (`sm`=40rem) — they don't correspond 1:1.** The detail header's title/OG-card row used viewport `sm:flex-row`, which still fired inside the split view's ~400px-wide right column and overlapped the title with the card. Converting to a container query needed `@xl` (36rem/576px), not `@sm`, to land between the split column's ~400px and the full page's ~720px. Caught only by rendering a screenshot, not by reading the class names.

**Still open:**
- **Public rollups: proposal sent, awaiting prompt-lab.** Not a bug — diagnosed 2026-07-19. Private `weekly_rollups` is current through 2026-07-13 for every project; the synthesizer is healthy. Publication stopped because prompt-lab `9863a52` (2026-06-13) removed `/handoff`'s auto-write and the review-gated replacement landed 2026-07-19 unrun. **Proposal in the handoff file:** split the public contract — auto-publish `(project, week_of, session_count, commit_count)` nightly for allowlisted projects, keep `public_summary` human-gated. The gate protects prose, but the sparkline is built purely from `session_count` (`lib/feed.ts:46-48`), which discloses activity level, not content. Free on our side: `category-tile.tsx:44` already falls back via `{e.summary ?? e.tagline}`, `feed-card.tsx:51` renders summary conditionally, and `public_summary` is already nullable. Allowlist asks: **add `split-recording`**, **drop `am-i-an-ai`** (site dropped lojong 2026-07-05); recountly / songscribe / bakerylouise deliberately held back (personal journal; songscribe now has a page but isn't in the ask; client site). Don't hand-scrub the 36-week prose backlog.
- **Tiered disclosure** (the garm plan): prompt-lab owes a design reply on a service-authenticated `/api/private_history`. **This site owns identity and calls prompt-lab with a shared service key** — prompt-lab is deliberately *not* becoming a garm consumer (it has no per-user identity). Then wire `lib/garm.ts` here. Two open decisions: garm's project namespace already has `ibuild4you` with 32 app-level grants (reusing it may over-grant), and this site needs an *unscoped* consumer key since it checks ~10 slugs.
- **New pages queued:** songscribe shipped (music, beta — page added). Bakery Louise still queued (products — client site, https://bakerylouise.com; repo private → no calendar). Needs `/ask`-sourced copy + Nico approval, and a framing call (his work vs. her business). Prompt already delivered to that repo's agent to add an `og:image` — the site ships none, so shares render pictureless. **garm as a page: deferred, not now.** Unresolved: is the recordings catalog its own page or already inside `split-recording.mdx`?
- **Issue #7** — visual refresh to the catalog's editorial system. **Part 1 merged (PR #10)**: `--text-faint` raised `--n-400 → --n-500` (now AA in both themes, 4.7:1 light / 6.1:1 dark) and the detail `<h1>` switched to serif. **Part 2 in flight on `visual-refresh-7-build`** (branch off main, work not yet started): mono-label adoption on the other meta lines, ledger row-rhythm on feed cards, full catalog editorial port — the mockup round the issue asks for. (Token-adoption refactor was investigated and is a zero-pixel no-op — aliases already resolve to the same flipping vars — so it was skipped.)
- **Issue #5** — richer card visuals. **Direction agreed 2026-07-20** (spec in the issue's pinned comment): curated static screenshots for key projects (esp. Art + Music — those repos are private/scrape-less), OG-scrape fallback for the rest via `cardImage`; static only for v1; richen `/[category]` feed cards + detail, keep home tiles text-dense; **sequence #7 → #5**, each with a mockup round. **Groundwork merged (PR #11)**: `cardImage` now overrides the OG scrape (`lib/feed.ts`, `components/og-preview.tsx`), `public/cards/<slug>.<ext>` convention documented — no visual change yet, no MDX sets `cardImage` today. Still needs Nico's asset creation (screenshots/photos) and the mockup round. https://github.com/nicolovejoy/selected-projects/issues/5
- **Issue #12** — split-pane live preview: embed the project's live site beside the detail content on `lg+` (opt-in `embed` frontmatter flag, iframe left ~2/3 + detail right ~1/3). Filed 2026-07-21, feasibility probed (musicforge/prntd/ibuild4you/rocksculpture frameable; recountly frames but redirects to login, off by default; site itself excluded). Stacks on the #7 part-2 branch. https://github.com/nicolovejoy/selected-projects/issues/12
- **Follower email digest** (Resend); **hardening** (docs/environments.md): preview email split → prod backup habit.
