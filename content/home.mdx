export const metadata = {
  title: "the piano house project",
};

This is a live, self-updating record of projects Nico is building with Claude Code. Updates are captured by the `/handoff` command at the end of each session — see the [Prompt Lab repo](https://github.com/nicolovejoy/prompt-lab) for how it works.
